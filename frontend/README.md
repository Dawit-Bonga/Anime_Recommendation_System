# Frontend

React + Vite client for Anime Match.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

Set backend URL in `.env` if needed:

```bash
VITE_API_URL=http://127.0.0.1:8000
```
